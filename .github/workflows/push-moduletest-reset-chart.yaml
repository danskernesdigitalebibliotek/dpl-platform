name: Publish moduletest-reset chart

on:
  push:
    branches:
      - main

jobs:
  publish:
    name: Publish moduletest-reset chart
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0 # Fetch all history for all branches and tags

      - name: Setup helm and yq
        uses: alexellis/arkade-get@7cc6ec1b6d8fcfcf3cc8cac995973147bc5fabf7
        with:
          helm: v3.17.3
          yq: v4.45.3

      - name: Log in to the Container registry
        uses: docker/login-action@371161bbe7024a29a25c5e19bfcbc0804fe9ad2c
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push helm chart
        env:
          VERSION: ${{ steps.version.outputs.value }}
        run: |
          VERSION=$(yq '.version' gitops/charts/moduletest-reset-cronjob/Chart.yaml)
          NAME=$(yq '.name' gitops/charts/moduletest-reset-cronjob/Chart.yaml)
          helm dependency update gitops/charts/moduletest-reset-cronjob || true
          helm package gitops/charts/moduletest-reset-cronjob
          helm push $NAME-$VERSION.tgz oci://ghcr.io/danskernesdigitalebibliotek/dpl-platform/charts
