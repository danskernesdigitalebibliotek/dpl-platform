apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: loki-ceph-bucket
  namespace: loki
spec:
  bucketName:
  generateBucketName: loki-ceph-bucket
  storageClassName: ceph-bucket
  additionalConfig:
    maxSize: "10G"
