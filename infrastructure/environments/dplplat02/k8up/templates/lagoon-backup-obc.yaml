apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: lagoon-bucket
  namespace: rook-ceph
spec:
  bucketName: lagoon-backup
  generateBucketName:
  storageClassName: ceph-bucket
